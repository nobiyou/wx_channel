# wx_channel 更新日志

## [5.7.9] - 2026-09-01

### 长时间运行与视频号页面恢复

- 增加页面应用层健康探测：连接建立后延迟探测，之后定期确认微信页面 API 仍可实际响应。
- 心跳改为确认机制，连续未收到确认时自动重连；服务端区分应用层活跃、协议层活跃和 API 功能状态。
- 修复 WebSocket 读取被固定空闲超时误断开的问题；协议连接仍存活但页面脚本失活时，自动通过 Windows 页面打开器恢复视频号页面。
- 保留旧连接事件保护、页面卸载处理、请求中的探测避让和健康状态上报，降低长时间运行后假死或首次请求失败。

### 视频号页面自动检查配置

- 新增 `auto_open_channels` 配置项，用于控制启动时的视频号页面自动打开与检查。
- 标准版和 Radar 版默认关闭，Cloud 版默认开启；构建脚本、配置样例和运行时行为保持一致。

### 长标题下载文件名

- 移除下载标题固定 50 字符限制，按目标目录和文件扩展名动态计算 Windows UTF-16 路径预算。
- 标题主体最多使用 180 个 UTF-16 单元，视频 ID、画质/分辨率后缀和扩展名优先保留，并避免截断代理对或非法尾字符。
- 批量、队列、分片合并、页面直连和历史任务恢复统一使用新命名规则；重名序号也会重新计算路径长度。
- 数据库和下载记录继续保存完整原始标题，文件名仅作为落盘时的安全投影。

### 公众号请求竞态修复

- 推送列表打开前等待公众号凭证刷新完成，避免首次请求在 `401 /api/mp/msg/list` 时失败。
- 公众号后台账号同步改为可复用的进行中 Promise，避免后台刷新与前台列表读取互相竞争。

## [5.7.8] - 2026-08-29

### 公众号资料库与历史文章管理

- 新增公众号账号资料库：打开已登录微信中的公众号文章或主页后，自动捕获账号元数据和短期会话，按 `biz` 去重并保存在本机。
- 新增公众号搜索和分页目录，可按公众号名称或 `biz` 查找已捕获账号，并查看文章数量、归档数量、同步状态和远程头像。
- 新增最近一页/全部历史文章同步，按页持久化游标、运行状态和错误信息，支持中断后继续、取消任务和重复同步去重。
- 新增文章归档：通过现有下载链路保存文章 HTML、图片等资源，生成本地 manifest 和相对路径，支持已归档内容复用与失败重试。

### 互动指标、评论与数据迁移

- 新增文章互动指标采集，保存阅读、点赞、评论、转发、收藏和赞赏的追加快照，并区分上游明确的 `0` 与未返回字段。
- 指标采集支持“采集待采集指标”和“刷新全部指标”，每篇文章完成后立即落库，支持断点续跑、有限重试、取消和幂等导入。
- 新增公众号目录 JSON/ZIP 导入导出，支持导入预检、冲突策略和文章/资源/指标状态迁移；导出不包含短期凭证。
- 扩展文章媒体字段和视频身份，补齐公众号历史文章中视频、音频、版权和恶意内容标记等元数据。

### 管理控制台与视频号稳定性

- Web 控制台新增“公众号资料库”页面、账号卡片、文章筛选、归档状态、指标状态和同步进度视图。
- 公众号页面底部菜单及视频号首页新增“管理控制台”入口，使用控制台图标并与下载入口分开，避免误触下载。
- 加固视频号页面 API 客户端的连接超时、心跳失败、旧连接事件和页面卸载处理，降低首次请求失败及断线后无法恢复的问题。
- 放宽内置视频号、公众号和本地控制台之间的明确来源白名单，保持页面注入和本地 API 调度可用。

## [5.7.7] - 2026-08-26

### 批量下载画质回退修复

- 修复批量下载在微信未提供可用 `fullUrl` 时仍按原始视频模式请求的问题。
- 批量入口与单个下载共用原始流判定；没有真实原始地址时自动选择最高可用 `spec` 画质，并保留完整 CDN 签名参数。
- 保留原始视频大小保护，疑似低码率或转码流不会被误保存为原片；新增 4.26 MB 对 0.25 MB 的回归覆盖。

## [5.7.6] - 2026-08-26

### Cloud/Insight Edge 保活与分享解析

- 修复 Cloud 客户端与 Insight Edge 的 WebSocket 心跳协议：Edge 返回同 ID 的 `heartbeat_ack`，客户端确认 ACK 后再刷新连接活跃时间，避免约 150 秒无业务消息时误断线。
- 增加 WebSocket 写入串行化和页面 API 心跳确认超时重连，降低心跳 ACK 与远程指令并发写入造成的连接异常。
- `/api/channels/status` 增加 `fresh_clients`、`schedulable_clients`、`last_pong_at`、平台可用状态和过期阈值；过期客户端不再参与 API 调度。
- 分享链接解析增加规范 HTTPS 地址校验、批量上限、重复 URL 去重、并发解析、单项/批次超时和结构化错误码。
- 统一无可用客户端、无就绪客户端和请求超时的错误识别，减少解析和搜索接口返回不一致。

## [5.7.5] - 2026-08-26

### 视频号下载链路修复

- 修复微信 CDN 原始流规则变化后，低码率转码文件被误标为“原始视频”的问题。
- 仅当微信页面数据明确提供 `fullUrl` 时才进入原始视频模式，不再把源文件大小提示当作原始地址证明。
- 微信未返回原始流时自动选择最高可用画质（当前桌面端优先 `xWT111`），并在下载界面明确提示实际模式。
- 保留完整签名 CDN URL，页面直连失败时自动回退后端下载，补齐 Referer、Origin 和用户代理。
- 增加原始视频大小校验，拦截明显低于源文件提示的转码流，避免生成“下载成功但不是原片”的文件。

## [5.7.4] - 2026-08-25

### 视频号页面生命周期管理

- 启动时检测 `Weixin.exe`；微信未运行时只等待，不启动或结束微信进程。
- 微信运行时通过 `weixin://dl/channels` 打开视频号页面，并由 wx_channel 负责页面刷新与恢复。
- 新增 `runtime.lifecycle` 诊断、定向页面刷新/导航命令和有界退避恢复。
- 移除页面固定 15 分钟自动刷新，保留评论导出刷新锁和手动刷新能力。
- Cloud 版继续使用 Insight Edge 默认链路，现有配置兼容不变。

### Prometheus 运行层退役

- 移除本地 Prometheus `/metrics` 端点、运行计数器、相关配置和直接依赖。
- 保留 `runtime.features.metrics_enabled=false` 兼容字段，以及 `/api/channels/status`、`/ws/health` 和 Insight 业务指标。

## [5.7.3] - 2026-08-24

### 启动更新要点同步

- 修正启动界面仍显示 v5.7.0 更新要点的问题，完整展示 Insight Edge 适配与 Hub 独有路径退役的五项说明。
- 启动更新要点改为独立版本化列表，并增加版本号、条目数量和旧文案残留检查，避免后续只更新发布文档而遗漏启动界面。
- 修复 UTC+8 凌晨时段“今日下载”和下载趋势可能把本地记录归到前一天的问题。
- 本补丁不改变 v5.7.2 的 Insight Edge 协议与运行行为。

## [5.7.2] - 2026-08-23

### Insight Edge 适配与 Hub 独有路径退役

- Cloud 版以 `wx_channel_insight` 的 Insight Edge 为默认远程数据面，继续兼容现有 `cloud_hub_url` 配置键和 `ws://127.0.0.1:18081/ws/client` 默认地址。
- 删除旧 Hub 设备绑定命令、`bind_token`、`hub_sync`、浏览/下载记录主动同步以及远端 Prometheus 指标推送，不再依赖停止开发的 `wx_channel_hub` 功能。
- 远程协议收敛为 `heartbeat`、`command`、`response`，继续保留 gzip 传输、页面能力心跳、远程 API 调用和断线重连。
- 本地 Prometheus `/metrics` 端点保持不变，配置样例、监控说明和启动日志统一使用 Insight Edge 语义。
- 保留本地 WebSocket Hub 作为页面客户端调度器；它属于 `wx_channel` 内部数据面，不是独立的 `wx_channel_hub` 项目。

## [5.7.1] - 2026-08-11

### 视频号分享链接解析改造

- 分享链接解析统一由已登录的微信桌面端视频号页面执行，移除 Cookie、Worker 和其他纯后端解析链路。
- Web 控制台分享链接导入统一使用视频号页面解析，并保留媒体地址、封面、解密 key、分辨率、时长和大小等信息。
- 使用微信桌面端实测 `https://weixin.qq.com/sph/AkRiXfmcFF` 解析成功。

## [5.7.0] - 2026-08-05

### Insight Hub 集成与 P0 稳定性优化

- Cloud 版改为连接 `wx_channel_insight` 内置的边缘网关，默认地址为 `ws://127.0.0.1:18081/ws/client`，显式配置仍优先。
- 增强 WebSocket 断线切换、可取消 API 调用和边缘节点能力状态同步。
- 评论导出改为后台任务，新增状态轮询接口，避免长 HTTP 请求完成后出现 `Failed to fetch` 误报。
- 评论导出增加分页上限、`.partial.json` 检查点和一级评论/回复/总数统计，真实导出成功后可直接核对结果。
- 独立 `wx_channel_hub` 不再作为运行依赖；雷达默认关闭，雷达能力继续通过独立雷达版提供。

## [5.6.9] - 2026-06-19

### 评论导出稳定性修复

- 评论导出改为分段落盘，导出过程中会先写入 `.partial.json` 检查点文件；若在拉取回复或长任务途中被中断，可保留当前进度，避免下次从头开始。
- 导出评论时会临时锁住页面保活脚本的 15 分钟自动刷新，减少长评论列表任务被页面刷新打断的概率。
- 修复“后端已成功导出评论，但前端仍弹出 `获取评论列表失败<Failed to fetch>`”的误报，成功与失败提示现在按真实链路分别展示。
- 评论导出成功提示补充一级评论数、回复数和总条数，便于直接核对导出结果。
- 同步更新版本号、README、版本说明、About 页面和 Windows 文件版本信息，保证发版展示一致。

## [5.6.8] - 2026-06-05

### 分享链接解析与控制台导入

- 修复 `/api/channels/shared_feed/profile` 分享链接详情链路，恢复历史上的 `finderGetCommentDetail` 主流程。
- 新增 `/api/channels/share/resolve` 分享链接解析接口，支持自动、视频号页面、Cookie/Worker 纯后端三种模式。
- Web 控制台批量下载页新增“分享链接下载”入口，可直接解析分享链接并写入下载列表。
- 设置接口补充 `sharedFeedBackendEnabled` 与 `sharedFeedBackendType`，前端可明确提示后端解析是否已配置。
- 补齐分享短链的 `eid` fallback，`get_feed_info` 失败时可直接回退到短链 ID，减少“之前能用，现在不能用”的回归。
- 同步更新版本号、启动横幅、README、版本说明与 Windows 文件版本信息。

## [5.6.7] - 2026-05-29

### 批量下载稳定性修复

- 加固批量下载文件处理流程：下载、解密、校验与最终落盘分阶段完成，减少异常中断后留下损坏成品文件。
- 修复部分批量下载视频无法播放的问题，重点收敛加密视频在解密失败或校验失败时的恢复与清理行为。
- 批量任务的暂停与恢复改为真实调用 Gopeed 任务级 `Pause` / `Continue`，不再用伪暂停状态重建任务。
- 优化批量下载并发场景下的任务状态一致性，降低“界面显示恢复但实际文件链路异常”的概率。
- 同步更新启动横幅、README、版本说明与 Windows 资源版本号，保证运行时和文档版本一致。

## [5.6.6] - 2026-05-26

### 配置回显与命名链路整理

- 下载文件名模板补齐 `{size}` 占位符，并统一批量下载、下载队列、队列转批量下载链路的命名元数据透传。
- `GET /api/settings` / Web 控制台设置页现在明确展示 `downloadFilenameTemplate` 与 `radarEnabled` 的配置回显语义。
- 雷达开关文档统一为“通过 `config.yaml` 中的 `radar_enabled` 显式控制，修改后需重启程序生效”。
- 启动阶段的日志初始化提示改为安全格式化输出，避免日志路径或提示文案中的 `%` 被误当成格式占位符。
- 同步更新 README、配置文档、控制台文档与 API 文档，避免继续出现“控制台即时启停雷达”的过时说明。

## [5.6.5] - 2026-05-15

### 原始视频、评论导出与命名策略更新

- 完善原始视频下载：原始视频下载完善，修复获取不到原始视频链接的问题。
- 优化评论导出：新增评论列表api，支持获取视频评论列表和回复分页。
- 下载命名优化：下载文件名默认不再附带视频 ID，同标题文件自动追加序号避免覆盖。
- 下载命名模板：新增 `download_filename_template` 配置项，支持 `{date} {datetime} {author} {title} {duration} {video_id} {size}`。
- 依赖补充：补充 `SunnyNet v1.0.3` 到项目 `pkg` 模块，便于后续开发和构建。

## [5.6.2] - 2026-04-26

### 文档与交付整理

- 清理已下线的视频号搜索页面相关代码与文档说明。
- 保留 API 搜索能力，不影响控制台和接口侧搜索调用。
- 调整仓库忽略规则，允许 `web/` 目录相关站点资源纳入版本管理。

## [5.6.1] - 2026-03-16

### 补丁修复

- 修复 `Home` 路径下分享直达视频、个人页跳转视频等“详情页模式”被误判为普通首页，导致下载按钮灰色不可用的问题。
- 修复 `Home` 路径下详情页模式点击“采集评论”提示“评论采集功能尚未就绪”的问题，补齐评论采集脚本注入。
- 优化 `Home` 页面直达视频链接识别，兼容 `oid/nid`、`context_id`、`fromSubPage=profile` 等不同 URL 形态。

## [5.6.0] - 2026-03-15

### 重点更新

- 适配微信视频号新版页面结构，重点修复 `Home / Feed / Profile / 赞和收藏(account/like)` 页面。
- 重构本地 WebSocket 与 Hub 设备选择逻辑，解决新版页面下“连上但不可用”导致的搜索和详情调用失败问题。
- 对标雷达默认改为关闭，并改为由 `config.yaml` 中的 `radar_enabled` 显式控制。

### 页面适配与稳定性修复

#### 首页 Home
- 适配新版顶部四标签结构：`直播 / 关注 / 朋友 / 推荐`
- 移除旧版左侧 12 模块相关残留逻辑
- 修复首页刷新卡在“加载中”、首屏空白等问题
- 改为更保守的首页数据同步方式，避免旧版 bundle 重写破坏新版页面初始化
- 首页切换视频后，重新输出当前视频详细信息，行为与 `feed` 页保持一致

#### Feed 页
- 适配新版 `feed` 页面头部与视频切换结构
- 修复切换上下一个视频后，下载仍指向上一条视频的问题
- 修复评论采集入口，支持先自动打开评论侧栏再开始采集
- 评论采集改为兼容新版侧栏 DOM 结构，支持从页面 DOM 直接解析评论与回复
- 优化评论采集进度提示与自动滚动逻辑，优先滚动评论区而非主视频区域
- 修复评论保存文件命名，使其与视频命名风格统一
- 后续版本已退役上述评论采集链路，改为统一的评论列表 API 获取方式

#### Profile / 赞和收藏
- 适配新版 `profile` 页面资料区按钮布局
- 批量下载按钮改为跟随新版原生操作区位置
- 修复 `account/like` 页面空白问题
- 支持“赞和收藏”页面按当前子标签单独显示批量下载内容，不再混合全部数据
- 修复滚动加载更多后批量下载数量不更新的问题

### WebSocket 与 Hub 联动增强

- 本地 `/ws/api` 增加客户端状态上报，包含：
  - 当前页面路径
  - 当前 URL
  - API 是否就绪
  - 支持的方法集合
- Hub 端记录并展示设备能力：
  - 搜索能力
  - 视频列表能力
  - 视频详情能力
  - ready 页面数量
- Hub 搜索、视频列表、视频详情调用改为优先选择真正具备对应能力的设备
- `/api/status` 返回更完整的客户端能力列表，便于排查“已连接但不可用”的问题
- Hub 前端 Dashboard / Search 页面增加能力状态展示，并优化“自动选择设备”逻辑

### 雷达与控制台

- 新增 `radar_enabled` 配置项，默认值为 `false`
- 程序启动时仅在显式启用时启动雷达服务
- 本地 Web 控制台“设置”页展示当前 `radar_enabled` 状态，但不负责持久化修改
- 雷达页面在未开启时明确提示需修改 `config.yaml` 并重启程序

### 兼容性说明

- 为保证新版视频号页面稳定，已停用部分旧版首页 JS 源码重写逻辑
- 新版页面优先采用运行时状态与页面结构识别，不再依赖高风险的旧 bundle 硬编码替换
